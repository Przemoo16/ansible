- name: Update repositories cache
  become: true
  ansible.builtin.apt:
    update_cache: yes
  tags:
    - always
- name: Install core system packages
  become: true
  ansible.builtin.apt:
    name:
      - curl
      - git
  tags:
    - always
