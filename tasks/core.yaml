- name: Update repositories cache
  become: true
  ansible.builtin.apt:
    update_cache: yes
  tags:
    - always
- name: Install system packages
  become: true
  ansible.builtin.apt:
    name:
      - build-essential
      - curl
      - git
  tags:
    - always

